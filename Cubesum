Given a number, how does one get the sum of the cubes of the digits? This program demonstrates how.

import java.util.*;
import java.io.*;

class Cubesum {
    public static void main(String args[]){
         int input = 0;
         int num1, num2, num3;
         
         System.out.println("Enter a number: ");
         Scanner console = new Scanner(System.in);
         input = Integer.parseInt(console.nextLine());
         
         int number = input; // this number is a temp variable
         int counter = 0; // used to count the number of digits
         
         while(number > 0){
             int t = number%10;
             System.out.println("Cube of "+t+" is "+(t*t*t));
             counter = counter + 1;
             number = number / 10;
             }
         }
     }
